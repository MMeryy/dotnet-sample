name: Hello World
on: [push] # The Trigger

jobs:
  #build-app: # The Job
    #runs-on: ubuntu-latest
    #steps:
      #- name: Say hello
       # run: echo "Starting the build..." # The Step
      # - name: Check version 
      #   run: node -v
  run-template:
    #uses: Reusable-Templates/.github/workflows/template1.yml
    uses: MMeryy/Reusable-Templates/.github/workflows/ci.yml@main
    with:
      environment: production
      debug_mode: true
      port_number: 8080
