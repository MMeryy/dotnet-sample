on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
   # secrets:
     # DOCKER_TOKEN:
       # required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploying to ${{ inputs.environment }}"
      #- run: docker login -u user -p ${{ secrets.DOCKER_TOKEN }}
