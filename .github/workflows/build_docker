name: docker_build
on: [push] # The Trigger
  
jobs:
  call-template: 
    uses: Reusable-Templates/.github/workflows/ci.yml
    with:
      environment: 'production'
    #secrets:
      #DOCKER_TOKEN: ${{ secrets.MY_PROD_TOKEN }}
